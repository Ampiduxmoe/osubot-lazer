{
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "20.8.2",
    "gts": "^5.2.0",
    "mocha": "^10.4.0",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "test": "mocha 'build/clean/test/**/*.test.js'"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "sqlite3": "^5.1.7",
    "vk-io": "^4.9.1"
  }
}
